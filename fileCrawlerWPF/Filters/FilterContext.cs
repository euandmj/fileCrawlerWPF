namespace fileCrawlerWPF.Filters
{
    public enum FilterContext
    {
        Resolution,
        Framerate,
        VideoCodec,
        AudioCodec,
        Name,
        Regex,
        Extension
    }
}
